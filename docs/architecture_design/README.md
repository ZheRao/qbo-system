## Architecture Overview

The system is divided into two fundamental layers:

- **Structural Layer**  
  Responsible for transforming raw data into stable, schema-consistent representations without introducing business meaning.

- **Semantic Layer**  
  Responsible for constructing meaning, inference, and business logic on top of structured data.